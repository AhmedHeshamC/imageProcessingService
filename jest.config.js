module.exports = {
  testEnvironment: 'node',
  testTimeout: 10000,
  // Detect open handles (like open connections) after tests
  detectOpenHandles: true,
  // Force Jest to exit after tests complete
  forceExit: true
};
